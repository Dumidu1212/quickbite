- name: SonarCloud Scan
  uses: SonarSource/sonarqube-scan-action@v5
  with:
    projectBaseDir: user-service   # change per workflow
  env:
    SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
    SONAR_HOST_URL: https://sonarcloud.io
